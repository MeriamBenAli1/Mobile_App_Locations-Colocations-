<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to read images from external storage for Android 13 and above -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />



    <!-- Storage permissions for older versions (max SDK 29) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileAppProjectTEKDev5SAE3"
        tools:targetApi="31"> <!-- Optional, remove if not needed -->

        <!-- Activities -->
        <activity
            android:name=".SearchAdActivity"
            android:exported="false" />
        <activity
            android:name=".PublishAdActivity"
            android:exported="false" />
        <activity
            android:name=".ViewAdsActivity"
            android:exported="false" /> <!-- Added if needed -->
        <activity
            android:name=".EditAdActivity"></activity>

        <!-- MainActivity with intent-filter for launching the application -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
